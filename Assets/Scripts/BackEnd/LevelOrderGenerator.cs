using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelOrderGenerator : MonoBehaviour {
    private ArrayList AlchemyLevels;
	// Use this for initialization
	void Start () {
        AlchemyLevels = new ArrayList();
         //har tänkt mig ha den här för att generera flera olika sets av levels men är inte relevant för Proof of Concept så kommer inte utvekla vidare.
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
